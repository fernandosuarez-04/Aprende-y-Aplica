'use client'

/**
 * ParticlesBackground - Removed for performance optimization
 * Previously contained animated particles that were causing performance issues.
 * Now returns null to eliminate rendering overhead.
 */
export function ParticlesBackground() {
  return null
}
