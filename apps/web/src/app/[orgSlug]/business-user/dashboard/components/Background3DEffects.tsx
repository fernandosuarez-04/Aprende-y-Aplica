'use client'

/**
 * Background3DEffects - Removed for performance optimization
 * Previously contained complex 3D effects that were causing performance issues.
 * Now returns null to eliminate rendering overhead.
 */
export function Background3DEffects() {
  return null
}
